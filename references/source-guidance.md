# GPT-5.6 来源指南

主要提供来源：用户于 2026-07-15 提供的《GPT-5.6 Sol 提示词指南》。

官方模型指南：<https://developers.openai.com/api/docs/guides/latest-model?model=gpt-5.6>

## 提炼原则

- 定义结果、重要约束、可用证据和完成门槛，然后为 GPT-5.6 保留选择高效路径的空间。
- 添加新的结构前，先删除重复指令、示例、过程规则和不相关工具。
- 保留用户明确给出的值。使用决策标准，而不是通用默认值或关键词映射。
- 保持人格和协作风格简短，并与目标、工具、成功标准和权限分开。
- 只定义一次自主权和审批边界。
- 只暴露相关工具；当正确性依赖前置信息时，要求先检索这些信息。
- 仅在有界的确定性归约阶段使用程序化工具调用。
- 为有依据的任务定义引用和证据缺失时的处理方式。
- 仅为多步骤工作添加少量进度更新。
- 使用与任务相关的测试、构建、渲染或证据检查验证输出。
- 根据代表性任务的要求设定推理强度和详细程度，不要全局设为最高值。

## 溯源边界

不要将提供的指南逐字复制到生成的提示词中。只提炼会改变行为的规则。除非用户提供或已从当前官方文档检索，否则将当前 API 限制、价格、功能可用性和不受支持的参数视为未核验。

